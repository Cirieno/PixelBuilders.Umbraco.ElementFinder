<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <!-- Path from the .targets file to the Core project's built frontend assets -->
    <ElementFinderSource>$(MSBuildThisFileDirectory)..\wwwroot\**\*.*</ElementFinderSource>
  </PropertyGroup>

  <Target Name="CopyElementFinderAssets" BeforeTargets="Build">
    <ItemGroup>
      <ElementFinderFiles Include="$(ElementFinderSource)" />
    </ItemGroup>

    <Message Text="--- ELEMENT FINDER: Copying assets from Core to Site ---" Importance="high" />

    <Copy
      SourceFiles="@(ElementFinderFiles)"
      DestinationFiles="@(ElementFinderFiles->'$(MSBuildProjectDirectory)\App_Plugins\RL_PixelbuildersUmbracoElementFinder\%(RecursiveDir)%(Filename)%(Extension)')"
      SkipUnchangedFiles="true"
    />
  </Target>
</Project>
